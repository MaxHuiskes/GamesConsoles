{{ form_start(form) }}
    {{ form_row(form.name) }}
    <div class="btn-group">
        <button type="submit" class="btn btn-primary">Save</button>
        <a href="{{ path('app_tag_index') }}" class="btn btn-secondary">Cancel</a>
    </div>
{{ form_end(form) }}
